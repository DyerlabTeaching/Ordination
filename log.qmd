---
title: "Teaching Log"
format: html
---

> Daily updates on progress for the course in each section.

## 2026-08-24

- `narrative.qmd`'s setup chunk has a commented-out `library(rgl)` block (3D plotting + `webgl` knitr hook) that was never re-enabled. Left disabled during this render pass because `rgl` requires the X11 subsystem, which isn't available in this environment. **TODO:** either install XQuartz/X11 and re-enable this block, or replace the 3D visualization with a non-`rgl` alternative (e.g. `plotly`) that doesn't need X11.

## Copy-Edit Log — Ordination

Tracks copy-editing passes over this module's `.qmd` documents. Clear-cut
typos/grammar/spelling errors are corrected directly in the source. Items
that need an author decision are left in place but wrapped in
`<mark>...</mark>` so they show up highlighted in the rendered HTML.

### narrative.qmd — 2026-08-15

#### Fixed directly (typos, spelling, grammar)

| Line | Issue | Fix |
|---|---|---|
| 24 | "aquatic vegitation" | "vegetation" |
| 48 | "look a the data" | "look at the data" |
| 48 | "there are a moderate amount" (agreement) | "there is a moderate amount" |
| 59 | "Principle Component Analyses" (heading) | "Principal Component Analyses" |
| 61 | "Principle component analysis (PCA)" | "Principal component analysis (PCA)" |
| 62 | "The number of axes ... are the same" (agreement) | "is the same" |
| 65 | "orthoganal" | "orthogonal" |
| 66 | "those who explain the least" (non-human antecedent) | "those that explain the least" |
| 68 | "exmaple" | "example" |
| 70 | "2-dimenational" (image caption) | "2-dimensional" |
| 91 | "underyling" | "underlying" |
| 99 | "coordiante" | "coordinate" |
| 113 | "Non-metric Multiple Dimensional Scaling" (heading — not the standard name) | "Non-metric Multidimensional Scaling" |
| 132 | `ISBN: ![978-0-471-88950-2](...)` — stray `!` turns the citation into a broken image embed instead of a link | `ISBN: [978-0-471-88950-2](...)` |

#### Flagged for your review

None — the rest of the document read cleanly.
